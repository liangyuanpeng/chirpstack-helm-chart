apiVersion: v1
kind: Service
metadata:
  name: {{ include "chirpstack-helm-chart.fullname" . }}
  labels:
    {{- include "chirpstack-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chirpstack-helm-chart.selectorLabels" . | nindent 4 }}
